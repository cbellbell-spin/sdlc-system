# File Structure Reference

## External Inputs (Read-Only)

The SDLC system reads from `~/Documents/pm-coach/` (the `product-ic-coach` repository) as an input source. This is **read-only** from the SDLC's perspective — the IC coach owns the canonical files.

```
~/Documents/pm-coach/
├── customer-context/
│   └── <segment-or-account-slug>/
│       ├── profile.md          # canonical, revised in-place
│       └── log.md              # append-only
├── problems/
│   └── <problem-slug>/
│       ├── problem-statement.md
│       ├── notes.md
│       └── links.md
└── prioritization/
    └── <yyyy-mm-dd>-<context>/
        └── snapshot.md
```

**Contract:** The IC coach's `customer-understanding` `profile.md` and `problem-statement.md` formats are the stable contract the SDLC reads from. Section headers are not just markdown for humans — they're the API the SDLC parses.

**When the SDLC references these inputs:**
- The Opportunity Brief (Phase 1A) reads `customer-context/<slug>/profile.md` to inform the problem hypothesis. See `references/phase-behavior.md` for the "Cross-Plugin Read" block.
- The PM's `_state.md` records the input paths under "Customer-context anchors" (see schema below).
- The SDLC does NOT copy, edit, or restate content from `~/Documents/pm-coach/` inline. It links.

---

## Portfolio Level (Shared Folder Root)

### `_portfolio_registry.md`

One row per initiative. The agent reads this at session start and updates it when phase, status, or PM changes.

```markdown
# Portfolio Registry

| Initiative | Slug | Named PM | Phase | Status | Last Updated |
|-----------|------|----------|-------|--------|--------------|
| [Name] | [slug] | [PM name] | [e.g., Phase 2A] | Active / At gate / Parked / Killed / Complete | [YYYY-MM-DD] |
```

**Status definitions:**
- **Active** — in-flight, being actively worked
- **At gate** — artifact complete, awaiting gate decision from leadership
- **Parked** — gate decision was Park; re-entry trigger is defined in `_gate_log.md`
- **Killed** — gate decision was Kill; recorded for organizational memory
- **Complete** — through Phase 6B with a Scale or sustained operating decision

---

## Per-Initiative Structure

```
initiatives/[slug]/
  _state.md
  _metric_registry.md
  _gate_log.md
  decisions.md
  01_opportunity_brief.md
  02_validated_opportunity_brief.md
  03_product_brief.md
  04_prd.md
  05_signal_report_01.md
  05_signal_report_02.md
  [signal reports numbered sequentially]
  06_phase5_rollout_decision_01.md
  [rollout decisions numbered sequentially]
  07_phase6a_decision_brief.md
  08_phase6b_operating_review_01.md
  [operating reviews numbered sequentially]
  openspec/
    changes/
      [slug]-v[N]/        ← active OpenSpec package, generated at Phase 4 completion
    archive/
      [slug]-v[N-1]/      ← prior versions preserved here
```

**`openspec/` directory:** Generated by the system at Phase 4 completion. Do not create manually. The `changes/[slug]-v[N]/` folder is the active engineering handoff package. See `references/openspec-handoff.md` for structure and content. Prior versions are preserved in `openspec/archive/` and must not be deleted — engineering may have active work against a prior version.

**Numbering rule for sequential artifacts:** Scan the folder for existing files. Tell the PM: "The last [signal report / rollout decision / operating review] filed was #[N]. Are we working on #[N+1]?" Create the file only after PM confirms.

---

## `_state.md` Schema

Written at the end of every session. The agent's working memory across sessions.

```markdown
# Initiative State: [Initiative Name]
**Named PM:** [Name]
**Slug:** [slug]
**Last updated:** [YYYY-MM-DD HH:MM]
**Current phase:** [e.g., Phase 2A — drafting Product Brief]
**Gate status:** [e.g., At Gate 1B — waiting for decision / Decision received — advancing to Phase 2A]

## Completed Artifacts
- [01_opportunity_brief.md — Approved Gate 1A YYYY-MM-DD]

## Customer-context anchors
Links to artifacts in `~/Documents/pm-coach/` (the `product-ic-coach` repository) that informed this initiative. These are **inputs**, not outputs. The initiative may have evolved beyond them; reference for lineage, but the canonical artifact is in `initiatives/<slug>/`.
- Customer context: `customer-context/<slug>/profile.md`
- Problem statement: `problems/<slug>/problem-statement.md`
- Prioritization snapshot: `prioritization/<date>-<context>/snapshot.md`
- Other: <free-form paths>

## In Progress
[What was actively being worked on at session end — artifact name and specific section]

## Pending from PM
[What the PM must do or provide before the next session — be specific.]

## Open Questions
| # | Question | Phase to resolve | Owner |
|---|---------|-----------------|-------|

## Flags
[Issues requiring attention. "None" is acceptable but must be explicit — do not leave blank.]

## Context Notes
[Things the PM mentioned that are not in any artifact but matter for continuity.]
```

---

## `_metric_registry.md` Schema

Written once at the end of Phase 2A approval. Read-only after that point.

```markdown
# Metric Registry: [Initiative Name]
**Locked:** [Date Phase 2A approved at Gate 2A]
**Source:** [03_product_brief.md]

## Behavioral Metrics

| Metric name | Definition | Telemetry event | Trigger condition | Storage location | Owner |
|------------|-----------|----------------|------------------|-----------------|-------|

## Financial Metrics

| Metric name | Definition | Source system |
|------------|-----------|---------------|

## Success Criteria

| # | Criterion | Observable threshold | Confidence |
|---|-----------|---------------------|------------|

---
*This registry is locked. Any change requires PM acknowledgment, explicit written justification, and logging in _gate_log.md as a metric continuity event.*
```

---

## `_gate_log.md` Schema

```markdown
# Gate Log: [Initiative Name]

| Gate | Date | Decision | Decision-maker | Conditions / Notes |
|------|------|----------|---------------|-------------------|
| Gate 1A | | Fund Validation / Kill | | |
| Gate 1B | | GO / CONDITIONAL GO / NO-GO / BET | | |
| Gate 2A | | Prioritize / Park / Kill | | |
| Gate 2B | | Fund / Park / Kill | | |
| Gate 2C | | [Timeline, release strategy] | | |
| Gate 3 | | Launch (Full) / Launch (Conditional) / Delay | | |
```

---

## `decisions.md` Schema

Written at initiative creation (empty header row only). Appended at the end of every session where a notable decision was made. Append-only — never edit or delete prior entries.

```markdown
# Decisions Log: [Initiative Name]

| # | Date | Phase | Decision | Alternatives considered | Rationale |
|---|------|-------|----------|------------------------|-----------|
```

**What belongs here:** Calls that shaped the work but aren't captured in the artifact itself — a scope item cut and why, a metric definition chosen over alternatives, a framing selected, a signal discarded with reason. Not process events (gate decisions go in `_gate_log.md`) and not state (that goes in `_state.md`).

**Entry guide:**
- `Decision` — the call made, stated plainly (e.g., "Cut SSO from v1 scope")
- `Alternatives considered` — what else was live before the call was made
- `Rationale` — why this call, not the alternatives (e.g., "Engineering estimate was 6 weeks; not justified by validated demand signal")

**The test:** Can a new session, reading only the project files, avoid re-litigating this decision? If yes, the entry is doing its job.
